# Local configuration (generated by the installer — never commit real credentials)
/config/config.php

# Uploaded media (keep the folder, ignore its contents)
/uploads/*
!/uploads/.htaccess
!/uploads/.gitkeep
!/uploads/thumbs/
/uploads/thumbs/*
!/uploads/thumbs/.gitkeep

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
